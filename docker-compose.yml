services:
  # shake-client:
  #   image: xanderstrike/shake-client:latest
  #   container_name: shake-client
  #   volumes:
  #     - ./demoq3:/usr/share/nginx/html/demoq3
  #   ports:
  #     - "8081:80"
  shake-dev:
    image: nginx:latest
    container_name: shake-dev
    restart: always
    environment:
      - VIRTUAL_HOST=shake.astandke.com
      - LETSENCRYPT_HOST=shake.astandke.com
      - VIRTUAL_PORT=80
    volumes:
      - ./client:/usr/share/nginx/html
      - ./demoq3:/usr/share/nginx/html/demoq3
      - ./client/nginx.conf:/etc/nginx/conf.d/default.conf
  shake-server:
    image: xanderstrike/shake-server:latest
    container_name: shake-server
    restart: always
    environment:
      - VIRTUAL_HOST=shake-peerserver.astandke.com
      - LETSENCRYPT_HOST=shake-peerserver.astandke.com
      - VIRTUAL_PORT=8080
